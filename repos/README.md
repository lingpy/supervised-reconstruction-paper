# Preparing the data from CLDF

Please refer to the two shell-scripts, `clone.sh` to clone the data with GIT, and `prepare.sh`, to prepare the data by extracting only relevant parts and converting data to CLDF. 
